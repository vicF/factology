// Transitional re-export shim — SyncEngine now lives in the engine package
// (packages/engine/src/sync/SyncEngine.js). Kept until all app imports move to
// @factology/engine (P1-S6), then this file is deleted.

export * from '@factology/engine/sync/SyncEngine.js';
