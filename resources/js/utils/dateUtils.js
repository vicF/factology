// Transitional re-export shim — date utilities now live in the engine package
// (packages/engine/src/utils/dateUtils.js). Kept until all app imports move to
// @factology/engine (P1-S6), then this file is deleted.

export * from '@factology/engine/utils/dateUtils.js';
